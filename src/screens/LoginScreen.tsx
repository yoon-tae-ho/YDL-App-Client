import { Box, Text } from "native-base";
import { FC } from "react";

const LoginScreen: FC = () => {
  return (
    <Box>
      <Text color="white">Login Screen!</Text>
    </Box>
  );
};

export default LoginScreen;
